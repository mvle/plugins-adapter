apiVersion: apps/v1
kind: Deployment
metadata:
  name: nemo-guardrails
  namespace: istio-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nemo-guardrails
  template:
    metadata:
      labels:
        app: nemo-guardrails
    spec:
      containers:
      - name: nemo-guardrails
        # Note: Specific tags can be found at https://quay.io/repository/opendatahub/odh-trustyai-nemo-guardrails-server?tab=tags
        image: quay.io/opendatahub/odh-trustyai-nemo-guardrails-server:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: CONFIG_ID
          value: rails-custom-action
        ports:
        - containerPort: 8000
        volumeMounts:
        - name: config-volume
          mountPath: /app/config/rails-custom-action
      volumes:
      - name: config-volume
        configMap:
          name: nemo-config
---
apiVersion: v1
kind: Service
metadata:
  name: nemo-guardrails-service
  namespace: istio-system
spec:
  selector:
    app: nemo-guardrails
  ports:
  - name: http
    port: 8000
    targetPort: 8000
  type: ClusterIP
